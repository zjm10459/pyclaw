# PyClaw 依赖包
# ==================
# 
# ⚠️ 注意：依赖已迁移到 pyproject.toml
# 
# 安装方式：
#   pip install -e .                    # 基础安装
#   pip install -e ".[performance]"     # 性能优化
#   pip install -e ".[channels]"        # 渠道集成
#   pip install -e ".[dev]"             # 开发工具
#   pip install -e ".[all]"             # 完整安装
#
# ==================

# 为了兼容性，保留简化的依赖列表
# 推荐使用 pyproject.toml

# 核心依赖（最小安装）
websockets>=12.0
aiohttp>=3.9.0
click>=8.1.0
pydantic>=2.5.0
python-dotenv>=1.0.0
json5>=0.9.14
pyyaml>=6.0

# LangChain 核心
langchain-core>=0.3.0
langchain>=0.3.0
langchain-text-splitters>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0

# LLM Provider
openai>=1.50.0
anthropic>=0.40.0
requests>=2.32.0

# 向量记忆
sentence-transformers>=2.2.0
numpy>=1.24.0

# 网页工具
beautifulsoup4>=4.12.0
